name: Build Lean 4.24 Docker image

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - lean4-dockerfiles/Dockerfile.lean4-24
      - .github/workflows/build-lean4-24.yml

env:
  IMAGE: ghcr.io/ldct/lean4
  TAG: v4.24

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: lean4-dockerfiles
          file: lean4-dockerfiles/Dockerfile.lean4-24
          push: true
          tags: ${{ env.IMAGE }}:${{ env.TAG }}
