name: Build Mathlib4 + Canonical Docker image

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - lean4-dockerfiles/Dockerfile.mathlib4-canonical
      - .github/workflows/build-mathlib4-canonical.yml

env:
  IMAGE: ghcr.io/ldct/mathlib4-canonical

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: lean4-dockerfiles
          file: lean4-dockerfiles/Dockerfile.mathlib4-canonical
          push: true
          tags: ${{ env.IMAGE }}:v4.27.0-rc1
