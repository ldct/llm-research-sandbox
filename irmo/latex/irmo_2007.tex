\documentclass[12pt]{article}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage[margin=1in]{geometry}
\begin{document}
\begin{center}
{\large\bfseries 20th Irish Mathematical Olympiad\\12 May 2007}
\end{center}
\bigskip\noindent\textbf{Paper 1}\bigskip
\begin{enumerate}
\item Find all prime numbers $p$ and $q$ such that $p$ divides $q + 6$ and $q$ divides $p + 7$.

\item Prove that a triangle $ABC$ is right-angled if and only if
\[
\sin^2 A + \sin^2 B + \sin^2 C = 2.
\]

\item The point $P$ is a fixed point on a circle and $Q$ is a fixed point on a line. The point $R$ is a variable point on the circle such that $P$, $Q$ and $R$ are not collinear. The circle through $P$, $Q$ and $R$ meets the line again at $V$. Show that the line $VR$ passes through a fixed point.

\item Air Michael and Air Patrick operate direct flights connecting Belfast, Cork, Dublin, Galway, Limerick and Waterford. For each pair of cities exactly one of the airlines operates the route (in both directions) connecting the cities. Prove that there are four cities for which one of the airlines operates a round trip. (Note that a round trip of four cities $P$, $Q$, $R$ and $S$, is a journey that follows the path $P \to Q \to R \to S \to P$.)

\item Let $r$ and $n$ be nonnegative integers such that $r \le n$.

\begin{enumerate}
\item Prove that
\[
\frac{n+1-2r}{n+1-r}\binom{n}{r}
\]
is an integer.

\item Prove that
\[
\sum_{r=0}^{\lfloor n/2 \rfloor} \frac{n+1-2r}{n+1-r}\binom{n}{r} < 2^{n-2}
\]
for all $n \ge 9$.
\end{enumerate}

(Note that $\binom{n}{r} = \dfrac{n!}{r!\,(n-r)!}$. Also, if $x$ is a real number then $\lfloor x \rfloor$ is the unique integer such that $\lfloor x \rfloor \le x < \lfloor x \rfloor + 1$.)
\end{enumerate}

\newpage
\begin{center}
{\large\bfseries 20th Irish Mathematical Olympiad\\12 May 2007}
\end{center}
\bigskip\noindent\textbf{Paper 2}\bigskip
\begin{enumerate}
\setcounter{enumi}{5}
\item Let $r$, $s$ and $t$ be the roots of the cubic polynomial
\[
p(x) = x^3 - 2007x + 2002.
\]
Determine the value of
\[
\frac{r-1}{r+1} + \frac{s-1}{s+1} + \frac{t-1}{t+1}.
\]

\item Suppose $a$, $b$ and $c$ are positive real numbers. Prove that
\[
\frac{a+b+c}{3} \;\le\; \sqrt{\frac{a^2+b^2+c^2}{3}} \;\le\; \frac{\frac{ab}{c} + \frac{bc}{a} + \frac{ca}{b}}{3}.
\]
For each of the inequalities, find conditions on $a$, $b$ and $c$ such that equality holds.

\item Let $ABC$ be a triangle the lengths of whose sides $BC$, $CA$, $AB$, respectively, are denoted by $a$, $b$, $c$, respectively. Let the internal bisectors of the angles $\angle BAC$, $\angle ABC$, $\angle BCA$, respectively, meet the sides $BC$, $CA$, $AB$, respectively, at $D$, $E$, $F$, respectively. Denote the lengths of the line segments $AD$, $BE$, $CF$, respectively, by $d$, $e$, $f$, respectively. Prove that
\[
def = \frac{4abc(a+b+c)\Delta}{(a+b)(b+c)(c+a)},
\]
where $\Delta$ stands for the area of the triangle $ABC$.

\item Find the number of zeros in which the decimal expansion of the integer $2007!$ ends. Also find its last non-zero digit.

\item Suppose $a$ and $b$ are real numbers such that the quadratic polynomial
\[
f(x) = x^2 + ax + b
\]
has no nonnegative real roots. Prove that there exist two polynomials $g$, $h$, whose coefficients are nonnegative real numbers, such that
\[
f(x) = \frac{g(x)}{h(x)},
\]
for all real numbers $x$.
\end{enumerate}
\end{document}
